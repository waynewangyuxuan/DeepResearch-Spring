/** @type {import('next').NextConfig} */
const nextConfig = {
  // Spec directory is at ../spec relative to dashboard/
  // Parsed at runtime via fs, no webpack alias needed
};

export default nextConfig;
